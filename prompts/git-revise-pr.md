---
description: Address reviewer feedback on an existing GitHub PR or GitLab MR
---

Read `references/revise.md` before acting. Before push, read `references/pre-submit.md`. Also read one of `references/github.md` or `references/gitlab.md`.

Actor gate: continue only when the primary state is `NEEDS_REVISION` and the authenticated user is the author or a current assignee. If `WRITE_NOT_AUTHORIZED`, stop without editing.
Reply rule: one `next step:` line, review handoff in `SKILL.md`.

$ARGUMENTS
